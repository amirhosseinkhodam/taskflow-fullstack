# TaskFlow Frontend

Tiny Angular + Tailwind UI for manually testing the Nest API.

## Run

From the repository root:

```bash
npm install
npm run start:frontend
```

Keep the backend running on `http://localhost:3000`. The Angular dev server proxies `/api/*` to the Nest routes.
